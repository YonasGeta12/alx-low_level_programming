#include "main.h"

/**
 * *_srccat - fills memory with a constant byte
 * @dest: copy to
 * @src: copy from
 *
 *Return: dest
 */
char *_strcat(char *dest, char b, unsigned int n)
{
	unsigned int i;
	
	for (i=0; i<n; i++)
	{
		src[i] = b;
	}
	return (dest)
}
	
